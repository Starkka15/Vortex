// prettier-ignore
/* eslint-disable */
// @ts-nocheck
/* auto-generated by NAPI-RS */

const { createRequire } = require('node:module')
require = createRequire(__filename);

const { readFileSync } = require("node:fs");
let nativeBinding = null;
const loadErrors = [];

const isMusl = () => {
  let musl = false;
  if (process.platform === "linux") {
    musl = isMuslFromFilesystem();
    if (musl === null) {
      musl = isMuslFromReport();
    }
    if (musl === null) {
      musl = isMuslFromChildProcess();
    }
  }
  return musl;
};

const isFileMusl = (f) => f.includes("libc.musl-") || f.includes("ld-musl-");

const isMuslFromFilesystem = () => {
  try {
    return readFileSync("/usr/bin/ldd", "utf-8").includes("musl");
  } catch {
    return null;
  }
};

const isMuslFromReport = () => {
  let report = null;
  if (typeof process.report?.getReport === "function") {
    process.report.excludeNetwork = true;
    report = process.report.getReport();
  }
  if (!report) {
    return null;
  }
  if (report.header && report.header.glibcVersionRuntime) {
    return false;
  }
  if (Array.isArray(report.sharedObjects)) {
    if (report.sharedObjects.some(isFileMusl)) {
      return true;
    }
  }
  return false;
};

const isMuslFromChildProcess = () => {
  try {
    return require("child_process")
      .execSync("ldd --version", { encoding: "utf8" })
      .includes("musl");
  } catch (e) {
    // If we reach this case, we don't know if the system is musl or not, so is better to just fallback to false
    return false;
  }
};

function requireNative() {
  if (process.env.NAPI_RS_NATIVE_LIBRARY_PATH) {
    try {
      nativeBinding = require(process.env.NAPI_RS_NATIVE_LIBRARY_PATH);
    } catch (err) {
      loadErrors.push(err);
    }
  } else if (process.platform === "android") {
    if (process.arch === "arm64") {
      try {
        return require("./libloot-nodejs.android-arm64.node");
      } catch (e) {
        loadErrors.push(e);
      }
      try {
        const binding = require("libloot-nodejs-android-arm64");
        const bindingPackageVersion = require("libloot-nodejs-android-arm64/package.json").version;
        if (
          bindingPackageVersion !== "0.29.6" &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
        ) {
          throw new Error(
            `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
          );
        }
        return binding;
      } catch (e) {
        loadErrors.push(e);
      }
    } else if (process.arch === "arm") {
      try {
        return require("./libloot-nodejs.android-arm-eabi.node");
      } catch (e) {
        loadErrors.push(e);
      }
      try {
        const binding = require("libloot-nodejs-android-arm-eabi");
        const bindingPackageVersion =
          require("libloot-nodejs-android-arm-eabi/package.json").version;
        if (
          bindingPackageVersion !== "0.29.6" &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
        ) {
          throw new Error(
            `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
          );
        }
        return binding;
      } catch (e) {
        loadErrors.push(e);
      }
    } else {
      loadErrors.push(new Error(`Unsupported architecture on Android ${process.arch}`));
    }
  } else if (process.platform === "win32") {
    if (process.arch === "x64") {
      try {
        return require("./libloot-nodejs.win32-x64-msvc.node");
      } catch (e) {
        loadErrors.push(e);
      }
      try {
        const binding = require("libloot-nodejs-win32-x64-msvc");
        const bindingPackageVersion = require("libloot-nodejs-win32-x64-msvc/package.json").version;
        if (
          bindingPackageVersion !== "0.29.6" &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
        ) {
          throw new Error(
            `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
          );
        }
        return binding;
      } catch (e) {
        loadErrors.push(e);
      }
    } else if (process.arch === "ia32") {
      try {
        return require("./libloot-nodejs.win32-ia32-msvc.node");
      } catch (e) {
        loadErrors.push(e);
      }
      try {
        const binding = require("libloot-nodejs-win32-ia32-msvc");
        const bindingPackageVersion =
          require("libloot-nodejs-win32-ia32-msvc/package.json").version;
        if (
          bindingPackageVersion !== "0.29.6" &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
        ) {
          throw new Error(
            `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
          );
        }
        return binding;
      } catch (e) {
        loadErrors.push(e);
      }
    } else if (process.arch === "arm64") {
      try {
        return require("./libloot-nodejs.win32-arm64-msvc.node");
      } catch (e) {
        loadErrors.push(e);
      }
      try {
        const binding = require("libloot-nodejs-win32-arm64-msvc");
        const bindingPackageVersion =
          require("libloot-nodejs-win32-arm64-msvc/package.json").version;
        if (
          bindingPackageVersion !== "0.29.6" &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
        ) {
          throw new Error(
            `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
          );
        }
        return binding;
      } catch (e) {
        loadErrors.push(e);
      }
    } else {
      loadErrors.push(new Error(`Unsupported architecture on Windows: ${process.arch}`));
    }
  } else if (process.platform === "darwin") {
    try {
      return require("./libloot-nodejs.darwin-universal.node");
    } catch (e) {
      loadErrors.push(e);
    }
    try {
      const binding = require("libloot-nodejs-darwin-universal");
      const bindingPackageVersion = require("libloot-nodejs-darwin-universal/package.json").version;
      if (
        bindingPackageVersion !== "0.29.6" &&
        process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
        process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
      ) {
        throw new Error(
          `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
        );
      }
      return binding;
    } catch (e) {
      loadErrors.push(e);
    }
    if (process.arch === "x64") {
      try {
        return require("./libloot-nodejs.darwin-x64.node");
      } catch (e) {
        loadErrors.push(e);
      }
      try {
        const binding = require("libloot-nodejs-darwin-x64");
        const bindingPackageVersion = require("libloot-nodejs-darwin-x64/package.json").version;
        if (
          bindingPackageVersion !== "0.29.6" &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
        ) {
          throw new Error(
            `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
          );
        }
        return binding;
      } catch (e) {
        loadErrors.push(e);
      }
    } else if (process.arch === "arm64") {
      try {
        return require("./libloot-nodejs.darwin-arm64.node");
      } catch (e) {
        loadErrors.push(e);
      }
      try {
        const binding = require("libloot-nodejs-darwin-arm64");
        const bindingPackageVersion = require("libloot-nodejs-darwin-arm64/package.json").version;
        if (
          bindingPackageVersion !== "0.29.6" &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
        ) {
          throw new Error(
            `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
          );
        }
        return binding;
      } catch (e) {
        loadErrors.push(e);
      }
    } else {
      loadErrors.push(new Error(`Unsupported architecture on macOS: ${process.arch}`));
    }
  } else if (process.platform === "freebsd") {
    if (process.arch === "x64") {
      try {
        return require("./libloot-nodejs.freebsd-x64.node");
      } catch (e) {
        loadErrors.push(e);
      }
      try {
        const binding = require("libloot-nodejs-freebsd-x64");
        const bindingPackageVersion = require("libloot-nodejs-freebsd-x64/package.json").version;
        if (
          bindingPackageVersion !== "0.29.6" &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
        ) {
          throw new Error(
            `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
          );
        }
        return binding;
      } catch (e) {
        loadErrors.push(e);
      }
    } else if (process.arch === "arm64") {
      try {
        return require("./libloot-nodejs.freebsd-arm64.node");
      } catch (e) {
        loadErrors.push(e);
      }
      try {
        const binding = require("libloot-nodejs-freebsd-arm64");
        const bindingPackageVersion = require("libloot-nodejs-freebsd-arm64/package.json").version;
        if (
          bindingPackageVersion !== "0.29.6" &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
        ) {
          throw new Error(
            `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
          );
        }
        return binding;
      } catch (e) {
        loadErrors.push(e);
      }
    } else {
      loadErrors.push(new Error(`Unsupported architecture on FreeBSD: ${process.arch}`));
    }
  } else if (process.platform === "linux") {
    if (process.arch === "x64") {
      if (isMusl()) {
        try {
          return require("./libloot-nodejs.linux-x64-musl.node");
        } catch (e) {
          loadErrors.push(e);
        }
        try {
          const binding = require("libloot-nodejs-linux-x64-musl");
          const bindingPackageVersion =
            require("libloot-nodejs-linux-x64-musl/package.json").version;
          if (
            bindingPackageVersion !== "0.29.6" &&
            process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
            process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
          ) {
            throw new Error(
              `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
            );
          }
          return binding;
        } catch (e) {
          loadErrors.push(e);
        }
      } else {
        try {
          return require("./libloot-nodejs.linux-x64-gnu.node");
        } catch (e) {
          loadErrors.push(e);
        }
        try {
          const binding = require("libloot-nodejs-linux-x64-gnu");
          const bindingPackageVersion =
            require("libloot-nodejs-linux-x64-gnu/package.json").version;
          if (
            bindingPackageVersion !== "0.29.6" &&
            process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
            process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
          ) {
            throw new Error(
              `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
            );
          }
          return binding;
        } catch (e) {
          loadErrors.push(e);
        }
      }
    } else if (process.arch === "arm64") {
      if (isMusl()) {
        try {
          return require("./libloot-nodejs.linux-arm64-musl.node");
        } catch (e) {
          loadErrors.push(e);
        }
        try {
          const binding = require("libloot-nodejs-linux-arm64-musl");
          const bindingPackageVersion =
            require("libloot-nodejs-linux-arm64-musl/package.json").version;
          if (
            bindingPackageVersion !== "0.29.6" &&
            process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
            process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
          ) {
            throw new Error(
              `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
            );
          }
          return binding;
        } catch (e) {
          loadErrors.push(e);
        }
      } else {
        try {
          return require("./libloot-nodejs.linux-arm64-gnu.node");
        } catch (e) {
          loadErrors.push(e);
        }
        try {
          const binding = require("libloot-nodejs-linux-arm64-gnu");
          const bindingPackageVersion =
            require("libloot-nodejs-linux-arm64-gnu/package.json").version;
          if (
            bindingPackageVersion !== "0.29.6" &&
            process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
            process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
          ) {
            throw new Error(
              `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
            );
          }
          return binding;
        } catch (e) {
          loadErrors.push(e);
        }
      }
    } else if (process.arch === "arm") {
      if (isMusl()) {
        try {
          return require("./libloot-nodejs.linux-arm-musleabihf.node");
        } catch (e) {
          loadErrors.push(e);
        }
        try {
          const binding = require("libloot-nodejs-linux-arm-musleabihf");
          const bindingPackageVersion =
            require("libloot-nodejs-linux-arm-musleabihf/package.json").version;
          if (
            bindingPackageVersion !== "0.29.6" &&
            process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
            process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
          ) {
            throw new Error(
              `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
            );
          }
          return binding;
        } catch (e) {
          loadErrors.push(e);
        }
      } else {
        try {
          return require("./libloot-nodejs.linux-arm-gnueabihf.node");
        } catch (e) {
          loadErrors.push(e);
        }
        try {
          const binding = require("libloot-nodejs-linux-arm-gnueabihf");
          const bindingPackageVersion =
            require("libloot-nodejs-linux-arm-gnueabihf/package.json").version;
          if (
            bindingPackageVersion !== "0.29.6" &&
            process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
            process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
          ) {
            throw new Error(
              `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
            );
          }
          return binding;
        } catch (e) {
          loadErrors.push(e);
        }
      }
    } else if (process.arch === "riscv64") {
      if (isMusl()) {
        try {
          return require("./libloot-nodejs.linux-riscv64-musl.node");
        } catch (e) {
          loadErrors.push(e);
        }
        try {
          const binding = require("libloot-nodejs-linux-riscv64-musl");
          const bindingPackageVersion =
            require("libloot-nodejs-linux-riscv64-musl/package.json").version;
          if (
            bindingPackageVersion !== "0.29.6" &&
            process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
            process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
          ) {
            throw new Error(
              `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
            );
          }
          return binding;
        } catch (e) {
          loadErrors.push(e);
        }
      } else {
        try {
          return require("./libloot-nodejs.linux-riscv64-gnu.node");
        } catch (e) {
          loadErrors.push(e);
        }
        try {
          const binding = require("libloot-nodejs-linux-riscv64-gnu");
          const bindingPackageVersion =
            require("libloot-nodejs-linux-riscv64-gnu/package.json").version;
          if (
            bindingPackageVersion !== "0.29.6" &&
            process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
            process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
          ) {
            throw new Error(
              `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
            );
          }
          return binding;
        } catch (e) {
          loadErrors.push(e);
        }
      }
    } else if (process.arch === "ppc64") {
      try {
        return require("./libloot-nodejs.linux-ppc64-gnu.node");
      } catch (e) {
        loadErrors.push(e);
      }
      try {
        const binding = require("libloot-nodejs-linux-ppc64-gnu");
        const bindingPackageVersion =
          require("libloot-nodejs-linux-ppc64-gnu/package.json").version;
        if (
          bindingPackageVersion !== "0.29.6" &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
        ) {
          throw new Error(
            `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
          );
        }
        return binding;
      } catch (e) {
        loadErrors.push(e);
      }
    } else if (process.arch === "s390x") {
      try {
        return require("./libloot-nodejs.linux-s390x-gnu.node");
      } catch (e) {
        loadErrors.push(e);
      }
      try {
        const binding = require("libloot-nodejs-linux-s390x-gnu");
        const bindingPackageVersion =
          require("libloot-nodejs-linux-s390x-gnu/package.json").version;
        if (
          bindingPackageVersion !== "0.29.6" &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
        ) {
          throw new Error(
            `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
          );
        }
        return binding;
      } catch (e) {
        loadErrors.push(e);
      }
    } else {
      loadErrors.push(new Error(`Unsupported architecture on Linux: ${process.arch}`));
    }
  } else if (process.platform === "openharmony") {
    if (process.arch === "arm64") {
      try {
        return require("./libloot-nodejs.openharmony-arm64.node");
      } catch (e) {
        loadErrors.push(e);
      }
      try {
        const binding = require("libloot-nodejs-openharmony-arm64");
        const bindingPackageVersion =
          require("libloot-nodejs-openharmony-arm64/package.json").version;
        if (
          bindingPackageVersion !== "0.29.6" &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
        ) {
          throw new Error(
            `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
          );
        }
        return binding;
      } catch (e) {
        loadErrors.push(e);
      }
    } else if (process.arch === "x64") {
      try {
        return require("./libloot-nodejs.openharmony-x64.node");
      } catch (e) {
        loadErrors.push(e);
      }
      try {
        const binding = require("libloot-nodejs-openharmony-x64");
        const bindingPackageVersion =
          require("libloot-nodejs-openharmony-x64/package.json").version;
        if (
          bindingPackageVersion !== "0.29.6" &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
        ) {
          throw new Error(
            `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
          );
        }
        return binding;
      } catch (e) {
        loadErrors.push(e);
      }
    } else if (process.arch === "arm") {
      try {
        return require("./libloot-nodejs.openharmony-arm.node");
      } catch (e) {
        loadErrors.push(e);
      }
      try {
        const binding = require("libloot-nodejs-openharmony-arm");
        const bindingPackageVersion =
          require("libloot-nodejs-openharmony-arm/package.json").version;
        if (
          bindingPackageVersion !== "0.29.6" &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK &&
          process.env.NAPI_RS_ENFORCE_VERSION_CHECK !== "0"
        ) {
          throw new Error(
            `Native binding package version mismatch, expected 0.29.6 but got ${bindingPackageVersion}. You can reinstall dependencies to fix this issue.`,
          );
        }
        return binding;
      } catch (e) {
        loadErrors.push(e);
      }
    } else {
      loadErrors.push(new Error(`Unsupported architecture on OpenHarmony: ${process.arch}`));
    }
  } else {
    loadErrors.push(
      new Error(`Unsupported OS: ${process.platform}, architecture: ${process.arch}`),
    );
  }
}

nativeBinding = requireNative();

if (!nativeBinding || process.env.NAPI_RS_FORCE_WASI) {
  try {
    nativeBinding = require("./libloot-nodejs.wasi.cjs");
  } catch (err) {
    if (process.env.NAPI_RS_FORCE_WASI) {
      loadErrors.push(err);
    }
  }
  if (!nativeBinding) {
    try {
      nativeBinding = require("libloot-nodejs-wasm32-wasi");
    } catch (err) {
      if (process.env.NAPI_RS_FORCE_WASI) {
        loadErrors.push(err);
      }
    }
  }
}

if (!nativeBinding) {
  if (loadErrors.length > 0) {
    throw new Error(
      `Cannot find native binding. ` +
        `npm has a bug related to optional dependencies (https://github.com/npm/cli/issues/4828). ` +
        "Please try `npm i` again after removing both package-lock.json and node_modules directory.",
      { cause: loadErrors },
    );
  }
  throw new Error(`Failed to load native binding`);
}

module.exports = nativeBinding;
module.exports.Database = nativeBinding.Database;
module.exports.File = nativeBinding.File;
module.exports.Filename = nativeBinding.Filename;
module.exports.Game = nativeBinding.Game;
module.exports.Group = nativeBinding.Group;
module.exports.Location = nativeBinding.Location;
module.exports.Message = nativeBinding.Message;
module.exports.MessageContent = nativeBinding.MessageContent;
module.exports.MetadataWriteOptions = nativeBinding.MetadataWriteOptions;
module.exports.Plugin = nativeBinding.Plugin;
module.exports.PluginCleaningData = nativeBinding.PluginCleaningData;
module.exports.PluginMetadata = nativeBinding.PluginMetadata;
module.exports.Tag = nativeBinding.Tag;
module.exports.Vertex = nativeBinding.Vertex;
module.exports.EdgeType = nativeBinding.EdgeType;
module.exports.EvalMode = nativeBinding.EvalMode;
module.exports.GameType = nativeBinding.GameType;
module.exports.isCompatible = nativeBinding.isCompatible;
module.exports.LIBLOOT_VERSION_MAJOR = nativeBinding.LIBLOOT_VERSION_MAJOR;
module.exports.LIBLOOT_VERSION_MINOR = nativeBinding.LIBLOOT_VERSION_MINOR;
module.exports.LIBLOOT_VERSION_PATCH = nativeBinding.LIBLOOT_VERSION_PATCH;
module.exports.liblootRevision = nativeBinding.liblootRevision;
module.exports.liblootVersion = nativeBinding.liblootVersion;
module.exports.LogLevel = nativeBinding.LogLevel;
module.exports.MergeMode = nativeBinding.MergeMode;
module.exports.MessageType = nativeBinding.MessageType;
module.exports.selectMessageContent = nativeBinding.selectMessageContent;
module.exports.setLoggingCallback = nativeBinding.setLoggingCallback;
module.exports.setLogLevel = nativeBinding.setLogLevel;
module.exports.TagSuggestion = nativeBinding.TagSuggestion;
